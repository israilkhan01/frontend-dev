import { LOGO_URL } from "../utils/constants";
const Title = () => (
    <div className="w-24">
      <img
        className="w-24"
        alt="logo"
        src={LOGO_URL}
      />
    </div>
  );
  export default Title;